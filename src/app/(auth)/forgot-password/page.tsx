import React from "react";

type Props = {};

const ForgotPassword = (props: Props) => {
  return (
    <div>
      ForgotPassword - used to create developer experience. nextjs ommit the
      (auth) name in the browser routing
    </div>
  );
};

export default ForgotPassword;
